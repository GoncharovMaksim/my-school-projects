export const NOTIFICATION_CRITERIA = {
  math: {
    // Минимальная оценка для учета записи
    minGrade: 4,

    // Критерии по уровням сложности и операторам
    // Пользователь должен выполнить ВСЕ операторы, но может комбинировать уровни
    difficultyLevels: {
      1: {
        requiredRecords: 3, // 3 записи на каждый оператор для 1 уровня
        operators: ['+', '-', '*', '/'],
      },
      2: {
        requiredRecords: 2, // 2 записи на каждый оператор для 2 уровня
        operators: ['+', '-', '*', '/'],
      },
      3: {
        requiredRecords: 1, // 1 запись на каждый оператор для 3 уровня
        operators: ['+', '-', '*', '/'],
      },
    },

    // Общие требования: пользователь должен выполнить ВСЕ 4 оператора
    // Каждый оператор может быть выполнен на любом уровне сложности
    requiredOperators: ['+', '-', '*', '/'],
  },

  english: {
    // Минимальная оценка для учета записи (любая оценка)
    minGrade: 4,

    // Минимальное количество записей за день
    minRecords: 1,
  },
};
